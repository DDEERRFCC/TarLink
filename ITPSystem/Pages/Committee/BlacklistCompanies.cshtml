@page
@model CommitteeBlacklistCompaniesModel
@{
    ViewData["CommitteePageTitle"] = "Blacklist Company";
}

<div class="committee-page">
<partial name="_CommitteeHeader" />
<partial name="_CommitteeNav" />

<div class="alert alert-warning mb-0">
    Placeholder: `blacklistcompany` table exists in DB design but is not mapped in `ApplicationDbContext` yet.
</div>
</div>
